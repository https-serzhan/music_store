# Migrations

Task 5 does not require persistent storage for generated random data.

No database is used for generated songs. Song data is generated in memory on the server from deterministic seeds.

This folder is intentionally present to satisfy the required project structure if migrations are later added for lookup tables.
